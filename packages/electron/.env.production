# Production defaults – committed to git, safe to be public.
# Override with .env.local or shell env for local testing.

# Fixed base URL for frontend bundle auto-updates (always points to the latest release).
# GitHub releases/latest/download/ is an official redirect that never changes.
FRONTEND_BUNDLE_URL=https://github.com/zhougaopeng/browser-agent/releases/latest/download

